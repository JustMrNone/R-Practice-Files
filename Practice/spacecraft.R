spacecraft <- data.frame(
  name = c("Voyager1", "Voyager2", "Pioneer10"),
  distance = c(163, 136, 80)
)

spacecraft

spacecraft$name
spacecraft$distance

spacecraft[1]

spacecraft[[1]]

spacecraft[, 1]